---
plugin: networktocode.nautobot.inventory
validate_certs: true

query:
  devices:
    name:
    primary_ip4:
      host:
    device_role:
      slug:
    site:
      slug:
    platform:
      name:

group_by:
  - device_roles
  - platforms

compose:
  ansible_host: primary_ip4.host
